/* ============================================================
   通用场景 CG 池
   ------------------------------------------------------------
   不属于某一段、哪一段都可能用得上的那些图：据点全景、终末降临的空镜、
   某角色的定妆半身……登记在这里，**全部事件段共用**，不必往每个
   SagaScene 里各抄一份。

   与「本段登记的图位」（SagaScene.cg）的分工：
     本段 cg  —— 只属于这一段。导演没点名时，兜底摆它的头一张。
     通用池  —— 哪一段都可能出现。**只在导演点名时才出**，
               不参与兜底 —— 否则每一段一铺开都先冒出一张通用图，很吵。

   素材照旧放 public/cg/<id>.webp|png|jpg（见该目录 README）。
   id 要全局唯一，别跟各段自己那批撞名。

   一条可以带 `cast`（角色 id）：带上之后，**只有本段出场阵容里有其中之一**
   才进候选 —— 免得模型在一个没有恋兔光的场面里把她的定妆图摆出来。
   不带 cast 的视为哪一段都能用。
   ============================================================ */

import type { CgRef } from './types'

/**
 * 通用图位。空数组 = 不启用（页面与提示词里整块不出现）。
 *
 * 加一条的写法：
 *   { id: 'cg-committee-hall', note: '委员会总部大厅 · 长长的圆桌与窗外的苍色天光' },
 *   { id: 'cg-hikari-stand',   note: '恋兔光的定妆半身', cast: ['hikari'] },
 */
export const CG_POOL: CgRef[] = []
